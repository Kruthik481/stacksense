[project]
name = "stacksense"
version = "1.0.0"
description = "RAG-based code assistant that indexes your codebase and answers questions using AI"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend"]
addopts = "-v --tb=short"
